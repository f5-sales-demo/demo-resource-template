---
title: 部署
description: Terraform 部署操作指南
i18n:
  sourceHash: 8a082ae58970
  translator: machine
---

import { Steps } from "@astrojs/starlight/components";

## 快速開始

<Steps>

1. 複製儲存庫

   ```bash
   git clone https://github.com/f5xc-salesdemos/REPLACE_REPO_NAME.git
   cd REPLACE_REPO_NAME/terraform
   ```

2. 複製範例變數檔案

   ```bash
   cp terraform.tfvars.example terraform.tfvars
   ```

3. 使用您的值編輯 `terraform.tfvars`

   ```hcl
   subscription_id = "your-azure-subscription-id"
   # TODO: Add component-specific required variables
   ```

4. 初始化並規劃

   ```bash
   terraform init
   terraform plan
   ```

5. 套用

   ```bash
   terraform apply
   ```

</Steps>

## 變數

TODO: 在此新增變數參考表。

## 輸出

套用後，Terraform 會顯示關鍵輸出，包括 `public_ip`、`ssh_command` 和 `deployer`。
